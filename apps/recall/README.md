# Recall web app

The product side of the demo: the memory dashboard, email sign-in, the OAuth2 consent screen at `/oauth/consent`, and a connected apps page at `/connections`.

Run from the repo root with `pnpm dev` (port 4200). Configuration comes from the shared `.env` at the repo root.
